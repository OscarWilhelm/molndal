const homeView = '/'
const planningView = "/plan"
const itemsView = "/items"
const signInView = "/signin"

// eslint-disable-next-line
export default {
  homeView,
  planningView,
  itemsView,
  signInView
}